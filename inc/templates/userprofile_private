<tr>
	<tr><td colspan="2"><hr /></td></tr>
    <td valign="top">
    {$lang->lastvisited}: {$profile[lastVisit]}<br />
    <p>
    	<strong>{$lang->lastleaves}</strong><br />
        <ul>{$leaves}</ul>
    </p>
    <p>
    	<strong>{$lang->lastlogs}</strong><br />
        <ul>{$logs}</ul>
    </p>
    </td>
    <td valign="top">
        {$lang->currqreportingstats}:
        <ul>
            <li>{$countall_current_quarterly} {$lang->intotal}</li>
            <li>{$countall_current_quarterly_unfinalized} {$lang->unfinalized}</li>
        </ul>
        <hr />
            {$lang->duereports}:
        <ul>
            {$due_reports_list}
        </ul>
        <hr />
            {$lang->lastfinalized}:
        <ul>
            {$last_reports_list}
        </ul>
    </td>
</tr>